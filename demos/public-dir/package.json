{
  "name": "public-dir-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm install && tsc && vite build --watch",
    "build": "pnpm install && tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^4.4.4",
    "vite": "^2.7.9",
    "vite-plugin-web-extension": "workspace:*"
  }
}
