{
  "name": "localization-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "pnpm i && vite dev",
    "build": "pnpm i && tsc && vite build"
  },
  "devDependencies": {
    "typescript": "^5.3.2",
    "vite": "^5.0.0",
    "vite-plugin-web-extension": "workspace:*"
  }
}
