{
  "name": "vue-demo",
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "pnpm i && pnpm vite",
    "dev:firefox": "pnpm i && pnpm TARGET=firefox vite",
    "start": "pnpm i && vite build --watch",
    "start:firefox": "pnpm i && TARGET=firefox vite build --watch",
    "build": "pnpm i && tsc && vite build",
    "build:firefox": "pnpm i && tsc && TARGET=firefox vite build",
    "build:watch": "pnpm build --watch",
    "build:watch:firefox": "pnpm build:firefox --watch",
    "validate": "web-ext lint -s dist"
  },
  "dependencies": {
    "lodash.debounce": "^4.0.8"
  },
  "devDependencies": {
    "@types/lodash.debounce": "^4.0.6",
    "@vitejs/plugin-vue": "^3.0.0",
    "@vue/compiler-sfc": "^3.2.25",
    "typescript": "^4.4.4",
    "vite": "*",
    "vite-plugin-web-extension": "workspace:*",
    "vue": "^3.2.25"
  }
}
